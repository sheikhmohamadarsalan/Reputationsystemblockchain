# Reputationsystemblockchain

Please unzip the code and then run following commands in the terminal.

$ npm install

$ truffle compile
$ truffle migrate

To check out the frontend on localhost:

$ npm run start
